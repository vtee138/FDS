# Public base URL của backend (dùng cho docs; trình duyệt gọi qua /api/auth proxy)
NEXT_PUBLIC_API_URL=http://localhost:4000
# URL nội bộ backend khi chạy docker compose (server-side fetch, proxy, getSession)
# BACKEND_INTERNAL_URL=http://backend:4000
